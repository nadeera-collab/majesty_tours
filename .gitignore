# macOS
.DS_Store
assets/.DS_Store

# Raw source material (not referenced by the website)
majesty contents/

# Editor
.vscode/

# Local-only tooling (sharp, for scripts/optimize-images.mjs).
# The deploy build is deliberately dependency-free.
node_modules/
package-lock.json
